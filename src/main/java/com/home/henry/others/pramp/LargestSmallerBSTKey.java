package com.home.henry.others.pramp;

public class LargestSmallerBSTKey {
    static class Solution {
        /*

        Node findLargestSmallerKey(int num, Node root) {
            if (root == null) {return null;}
            Node res = null;
            if (root.key >= num) {
                res = findLargestSmallerKey(num, root.left);
            } else {
                res = findLargestSmallerKey(num, root.right);
            }
            if (res == null) {
                return root.parent;
            } else {
                return res;
            }
        }

         */
    }
}
